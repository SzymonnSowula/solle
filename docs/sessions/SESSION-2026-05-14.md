# Volle — Session 2026-05-14

## Co zrobiono wcześniej (agenci 1 & 2)

- **Agent 1 (voice/frontend)**: Web Speech API w App.svelte — push-to-talk spacja, TTS po polsku, orb z 4 stanami, fallback text input, auto-reconnect WS.
- **Agent 2 (backend/MCP)**: Real MCP client z SDK stdio, LangGraph orchestrator 5-węzłowy, 2 z 5 MCP serwerów z kodem (business-data + web-research), safety tiers.

## Co zrobiono teraz

### 1. Naprawa MCP transportu
- `mcp.client.stdio.stdio_client` zawieszał się w tym środowisku (anyio cancel-scope bug).
- Przepisano `mcp_client.py` na lekki `_SubprocessMCPClient` przez `asyncio.create_subprocess_exec` + JSON-RPC stdin/stdout.
- Naprawiono ścieżkę do pythona — teraz używa venv pythona (`_VENV_PYTHON`).

### 2. Fallback bez LLM
- Dodano `_fallback_understand()` w `orchestrator.py` — keyword routing gdy brak API key.
- Dodano `_format_tool_results()` — generowanie naturalnego tekstu po polsku z wyników tooli bez LLM.
- `call_tools` parsuje JSON stringi do dictów dla downstreamu.

### 3. LangGraph → run_pipeline
- LangGraph v1.2 wchodził w nieskończoną pętlę z async nodes.
- Dodano `run_pipeline()` — sekwencyjny pipeline: load_memory → understand → route → call_tools → generate_response → build_card.
- WebSocket w `main.py` używa teraz `run_pipeline`.

### 4. Poprawki UI
- `build_card` obsługuje teraz wyniki `compare_periods` (zamówienia/przychód z obu okresów + różnica %).

### 5. Konfiguracja
- Stworzono `.env.example` z `OPENAI_API_KEY`, `OPENAI_BASE_URL`, `OPENAI_MODEL`.

### 6. Testy E2E
- **Business-data MCP**: 11 tooli aktywnych, `get_sales_summary` zwraca 1905 zamówień / 194k PLN.
- **WebSocket sales query**: `Porównaj sprzedaż wczoraj z całym miesiącem` → tekst + karta wizualna.
- **WebSocket research query**: `Co nowego w polskim e-commerce?` → wyniki DuckDuckGo.

## Co jeszcze przed nami
- [ ] Klucz API LLM — wystarczy skopiować `.env.example` do `.env` i wpisać klucz
- [ ] Prawdziwe nagrywanie audio z mikrofonu (push-to-talk → Opus streaming)
- [ ] Deepgram STT + ElevenLabs TTS (zastąpić Web Speech API)
- [ ] Wake word (openWakeWord)
- [ ] Instalator Windows (.exe)

## Commit
- `20b5086` fix: MCP subprocess transport + fallback routing + run_pipeline + .env.example
