# Volle — Session 2026-05-14

## Co zrobiono dzisiaj

### 1. Bootstrap monorepo
- Utworzono strukturę `/home/szymon/volle/`
- Inicjalizacja Git + pierwszy commit
- Stworzono Obsidian Vault auto-sync (git hook `post-commit`)

### 2. Backend Python (`apps/agent-worker/`)
- **Analytics engine**: czyta mock CSV (`data/mock/sales_data.csv`), liczy:
  - zamówienia/przychód z wczoraj
  - średnią dzienną z bieżącego miesiąca
  - % różnicy
- **LLM module** (`agent/llm.py`): OpenAI-compatible API (OpenRouter/OpenAI).
  - System prompt zdefiniowany (polski, biznesowy, naturalny)
  - JSON mode dla strukturalnych odpowiedzi
  - Fallback do mock textu gdy brak klucza API
- **WebSocket endpoint** `/ws/voice`:
  - Intent routing dla słów kluczowych sprzedażowych
  - Testowane: odpowiedź z tekstem + visual_card w < 1s
- **Venv + deps**: FastAPI, pandas, httpx, python-dotenv

### 3. Desktop Tauri (`apps/client-desktop/`)
- **Tray icon**: menu Show / Hide / Quit
- **Global hotkey**: `Ctrl+Shift+Space` (Rust-side, działa z poziomu systemu)
- Okno startuje ukryte (widget on-demand)
- **UI (Svelte)**: orb z animacjami, karta metryk, WebSocket do backendu
- `cargo check` przechodzi pomyślnie

### 4. Mock dane biznesowe
- `data/mock/generate_sales.py`: 60 dni zamówień
- Ostatni dzień sztucznie podbity (+35%)
- Showcase query: "porównaj sprzedaż wczoraj z miesiącem"

### 5. Test end-to-end
```bash
curl http://localhost:8000/health
# {"status":"ok","llm_ready":false}

# WebSocket test (JSON utterance)
# -> "Wczoraj był dobry dzień. Zrealizowaliśmy 65 zamówień
#     na kwotę 5,619 złotych. To o 14% wyżej od średniej..."
```

## Co działa
- [x] Monorepo struktura
- [x] Backend WebSocket + analytics (pandas)
- [x] Frontend UI (orb + karta)
- [x] Tauri tray + global hotkey
- [x] Mock dane sprzedażowe
- [x] Obsidian auto-sync

## Co jeszcze przed nami
- [ ] Klucz API LLM (OpenRouter/OpenAI) — wystarczy dodać `.env`
- [ ] Prawdziwe nagrywanie audio z mikrofonu (push-to-talk)
- [ ] Audio streaming WebSocket (Opus)
- [ ] Wake word (openWakeWord)
- [ ] STT (Deepgram) + TTS (ElevenLabs)
- [ ] MCP serwery — podłączenie do agenta
- [ ] Instalator Windows (.exe)

## Notatki
- WSL brak display server → Tauri dev musi być na Windows host (PowerShell).
- Voice runtime (Rust/cpal/ONNX) wymaga `cmake libasound2-dev` w WSL.
- Demo "sprzedaż wczoraj vs miesiąc" działa jako JSON-only pipeline.
